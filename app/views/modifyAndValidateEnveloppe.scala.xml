@(shopId: String,
  requestId: String,
  transDate: javax.xml.datatype.XMLGregorianCalendar,
  ctxMode: String,
  authToken: String,
  comment: String,
  uuid: String
)
enveloppeAndHeader(shopId, requestId, transDate, ctxMode, authToken){
    <tns:validatePayment>
        @if(comment.nonEmpty){
        <tns:commonRequest>
            <tns:comment>@comment</tns:comment>
        </tns:commonRequest>
        }
        <tns:queryRequest>
            <tns:uuid>@uuid</tns:uuid>
        </tns:queryRequest>
    </tns:validatePayment>
}
